~canvas.window.view.keyDownAction = { arg v, char, m, u, k;
	//[v, c, m, u, k].postln;
/*
	if(char == $f, {
		~canvas.window.fullScreen;
	});
	if(char == $e, {
		~canvas.window.endFullScreen;
	});
	*/
	
	if(char == $p, {
		~canvas.play;
	});
	
	
};