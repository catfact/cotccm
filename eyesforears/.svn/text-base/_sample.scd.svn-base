SynthDef.new(\loopfade_s, {arg buf=0, amp=0, amplag= 30.0, rate=1.0, out=0;
	var sig;
	amp = Lag.kr(amp, amplag);
	sig = PlayBuf.ar(2, buf, rate:rate, loop:1);
	Out.ar(out, Pan2.ar(sig * amp, 0));
}).send(s);

0.01.wait;

~sampbuf = Buffer.read(s, "/Users/e/_svn/cotccm/eyesforears/Ib.wav");
0.1.wait;

~chimes_1 = Synth.new(\loopfade_s, [\buf, ~sampbuf.bufnum], s);
~chimes_1.set(\amp, -12.dbamp);